import React from 'react'

const Logout = () => {
  const logoutHandler = () => {
    // ログアウト処理を書く
  }

  return (
    <div>
      <button onClick={logoutHandler}>ログアウト</button>
    </div>
  )
}

export default Logout
